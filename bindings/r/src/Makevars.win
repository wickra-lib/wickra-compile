# On Windows the C ABI DLL is linked directly (mingw resolves the import stubs
# from wickra_compile.dll); the loader finds it via PATH at run time.
PKG_CPPFLAGS = -I$(WKCOMPILE_INC)
PKG_LIBS = -L$(WKCOMPILE_LIB) -l:wickra_compile.dll
