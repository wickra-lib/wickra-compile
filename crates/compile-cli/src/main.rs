//! `wickra-compile` — the reference codegen CLI.
//!
//! Scaffold stub; argument parsing and the generate/build/manifest flow land in
//! P-COMP-2.

fn main() {
    println!("wickra-compile {}", compile_core::version());
}
